# Deck Install Surface

Use `marketplace-index.json` as the external deck marketplace source.

```text
https://its-ze.github.io/tdongle-usb-adapter/deck/marketplace-index.json
```

`voidlink.pack.json` is intentionally standalone. Importing it should add the install card and app metadata without editing the existing built-in deck app list.
